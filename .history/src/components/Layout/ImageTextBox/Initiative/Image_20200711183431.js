import React from 'react';
import PBLogo from '../../../Logo/Logo';

const Image = (props) => {
    return (
        <div class='Image'>
            <img src={PBLogo} />
        </div>
    )
}

export default Image;